<!-- components/imgText/imgCmp.wxml -->
<view class="containter">
    <view>
        <view class="main-title">{{mainTitle}}</view>
        <view class="sub-head">{{subHead}}</view>
    </view>
    <view class="img-container">
        <image class="image" src="{{imgSrc}}" />
    </view>
</view>